services:
  ubwg-bot:
    image: ghcr.io/eschanet/ubwg-bot:0.0.2
    env_file:
      - path: .env
        required: false
    environment:
      - TELEGRAM_BOT_TOKEN
      - TELEGRAM_CHAT_ID
      - PRODUCT_URL
      - CHECK_INTERVAL_SECONDS
      - FAILURE_ALERT_THRESHOLD
    volumes:
      - ./state.json:/app/state.json
    restart: unless-stopped

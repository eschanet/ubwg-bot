services:
  ubwg-bot:
    image: ghcr.io/eschanet/ubwg-bot:0.0.1
    env_file:
      - path: .env
        required: false
    environment:
      - TELEGRAM_BOT_TOKEN
      - TELEGRAM_CHAT_ID
    volumes:
      - ./state.json:/app/state.json
    restart: unless-stopped
